/** Blueprint canvas tokens, mirroring the CSS custom properties in docs/DESIGN.md. */
export const theme = {
  surface: "#0f151b",
  gridLine: "rgba(63, 208, 224, 0.08)",
  rectStroke: "#3fd0e0",
  rectFill: "rgba(63, 208, 224, 0.08)",
  text: "#dce8ec",
  textMuted: "#6f8894",
  gridStep: 8,
} as const;
